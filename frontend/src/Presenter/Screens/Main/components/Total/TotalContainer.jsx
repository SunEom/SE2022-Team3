import React from "react";
import TotalPresenter from "./TotalPresenter";

const TotalContainer = ({ clothList }) => {
  return <TotalPresenter clothList={clothList} />;
};

export default TotalContainer;
